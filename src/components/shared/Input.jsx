function Input({ placeholder, className, onChange }) {
  return (
    <input
      placeholder={placeholder}
      className={className}
      onChange={onChange}
    ></input>
  );
}

export default Input;
